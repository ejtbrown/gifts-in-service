# GitHub deploy IAM module compatibility note

GitHub OIDC trust and least-privilege bootstrap roles live in `infra/bootstrap`, where repository identity is available before environment state exists.
