# Budget module compatibility note

Budget resources are implemented in `modules/observability` so their alarms and dashboard outputs remain one operational unit. This directory is retained as the documented module boundary for a future split.
